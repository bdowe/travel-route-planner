# sqlc query files live here (one .sql per feature, e.g. users.sql, trips.sql).
# Empty until the first feature (user-accounts) adds queries; `make api-sqlc`
# then generates the `store` package from these + the migrations schema.
